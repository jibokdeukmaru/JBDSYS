# JBDSYS ERP (지복득ERP) — 작업 규칙

- 이 저장소의 배포 대상 파일(`index.html` 등)을 수정한 뒤에는 **매번 확인을 구하지 않고 자동으로
  `git add` + `git commit` + `git push`까지 진행한다.** 2026-09-05에 사용자가 명시적으로
  "자동배포도 항상 적용해줘"라고 요청한 표준 워크플로우다.
- 이 저장소는 `CNAME` 파일이 있는 GitHub Pages 배포 구조라, `main`에 push하면 곧바로 라이브
  (`sys.jibokdeukmaru.com` 등 실제 운영 중인 ERP)에 반영된다 — 실사용자가 있는 프로덕션이다.
- 위 자동배포 원칙에도 불구하고, 아래 경우엔 push 전에 반드시 먼저 사용자에게 확인한다:
  - 되돌리기 어려운 구조적 변경(대규모 데이터 마이그레이션, Firestore 컬렉션 삭제/스키마 변경 등)
  - 코드에 구문 오류가 있거나 검증되지 않은 상태로 보이는 경우
  - 사용자가 별도로 "아직 올리지 마" 등으로 보류를 요청한 작업
- 커밋 메시지는 이 저장소의 기존 커밋 스타일(한국어, 한 줄 요약형, 필요시 " - "로 부연)을 따른다.
- `git remote -v`의 origin URL에 GitHub PAT(personal access token)이 그대로 박혀있다 — 이 토큰을
  로그, 메모리 파일, 커밋 메시지 등 어디에도 그대로 복사/노출하지 않는다.
